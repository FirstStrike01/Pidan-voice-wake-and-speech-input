# 皮蛋语音助手 · 更新日志（CHANGELOG）

> 记录「皮蛋语音助手」语音输入部分从零到现在的完整演进。
> 相关代码都在 `voice-wake/` 目录下，按阶段演进如下。

## 阶段一：离线唤醒词（VOSK）

**`wake_word.py`**

- 完全离线：对着麦克风喊「皮蛋」，识别到就输出 `1`。
- 用 VOSK 小模型 `vosk-model-small-cn-0.22`。
- 关键技巧：加了同音字兜底（皮蛋 / 皮带 / 皮旦…），解决 VOSK 听写生僻词容易认错的问题。

## 阶段二：离线语音输入（VOSK）

**`speech_input.py`**

- 持续监听，实时转文字，说完一句输出一句，并缓存写入 `transcript.txt`。
- 完全离线、免费，但**准确率不稳定**，尤其长句和生僻词。

## 阶段三：在线语音输入（阿里云百炼 DashScope）

**`speech_input_online.py`**

- 识别引擎换成阿里云百炼 `paraformer-realtime-v2`（联网、实时流式）。
- 准确率大幅提升。
- 新问题：**句首的「皮蛋」会被云端吞掉、或认成「皮带」**，不适合直接当唤醒词。

## 阶段四：方案 A 混合双引擎

**`pidan_assistant_v2.py`**

- **VOSK 管唤醒词（离线、免费、同音字兜底），DashScope 管指令转写（在线、准）**。
- 加「预录缓冲」，防止「皮蛋 + 指令一口气说」丢开头。
- 遗留 bug：云端的「句尾」和程序的「超时」打架，导致唤醒后约 1 秒就结束、还会发一个句号。

## 阶段五：v3 重构（当前最终版）

**`pidan_assistant_v3.py`**

- 修复「发空句 / 句号」：加 `is_content` 过滤纯标点、空结果。
- 重写超时策略为三段状态机：**待机 → 聆听（等第一句）→ 转化（停顿后结束）**。
- 微调时长（聆听 2 秒、停顿 1 秒）。
- **删除预录缓冲**：解决「皮蛋的『蛋』字漏进指令」问题，顺带利用云端的「句首吞字」反向吃掉蛋字。
- **去掉实时逐字显示**：改为唤醒后显示一次「识别中」，识别完输出完整指令。
- 修复注释腐烂（数值与注释对不上）。

## 其他脚本

- `setup_hotword.py`：（方案 B，可选）把「皮蛋」等加进阿里云定制热词表，提升在线识别率。


---

### 演进主线一句话

> 离线唤醒 → 离线语音输入 →（准确差）→ 在线语音输入 →（句首吞字）→ **本地唤醒 + 在线转写** → 细节打磨（超时、去预录、去实时显示）。
